<%- include('partials/header') %> 
    <div class="col-10 text-center mt-5 mx-auto ">
      <p class="mt-5"><%- messages('partials/messages', locals) %></p>
    <h2 class="mt-5"> <%= article.title %> </h2>
    <small class="mt-5">posted by <%= article.author %> </small>
    <p class="mt-5"><%= article.body %> </p>
  </div>
<div class="mt-5">
  <a class="btn btn-info btn-sm float-left" href="<%= article._id %>/edit">edit</a>
  <form method="POST" action='/<%= article._id %>/delete?_method=DELETE' class="my 5">  
  <button class="btn btn-danger btn-sm float-right" type="submit">Delete</button>
  </form>
</div>
         
<%- include('partials/footer') %>